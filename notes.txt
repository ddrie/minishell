read command -> lexer -> pareser -> execute -> post actions

built in - not in the system but are in shell (pwd -> getcwd)
ms$(prompt) pwd(biultin) args
int builtin_pwd(args)
1. fork
2. if child
3. prepare
4. postactions
5.exit

exit 1 arg
exit n
n%256

export {0}{6}
declare -x A [="B"]
-x - плвышение переменной до уровня окружения
переменные окружения

2 массива strings_name [pwd], [...] - у каждого инлексы
ptts - указатели на функцииб какую функцию нужно вызвать на какой индекс из 1 массива [builtin_pwd], [builtin_exit], [...]
int builtin_ptr(list arg) - типизированные парамерты у каждой функции в массиве

echo [-N][args]
echo $(pwd)
echo A -n: A -n\
echo -n A: A bash$

cd [args = путь] или без аргументов

unset
env

readline(line)
line != NULL ->add_history
rl_clear_history

Lexer вход: строка, выход:список токенов - превращает набор строки в список токенов, все что внутри кавычек - токен
token {int len, char *begin, int type}

1.00.00 - видео парсер